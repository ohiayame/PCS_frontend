services:
  frontend:
    build:
      context: ./entrance
      dockerfile: ../Docker/Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./entrance:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
    command: npm run dev -- --host
